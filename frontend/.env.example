# QueueMind Frontend Environment Configuration
# Copy this file to .env.local for local development.

# FastAPI Backend URL
# This is exposed to the browser to make client-side fetch requests.
# Do NOT hardcode the future Cloud Run URL here for production builds. 
# It should be injected at build time.
NEXT_PUBLIC_API_BASE_URL="http://localhost:8000"
